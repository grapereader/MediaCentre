#ifndef AUTHROUTES_H
#define AUTHROUTES_H

#include "vmc-libserver/Router.h"

namespace vmc
{
    namespace routes
    {
        namespace auth
        {
            void route(Router &router);
        }
    }
}
#endif
